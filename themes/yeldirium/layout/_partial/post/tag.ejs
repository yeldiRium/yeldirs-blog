<% if (post.tags && post.tags.length > 0){ %>
  <span class="post__tag">
    <i class="fas fa-tags"></i>
    <%- list_tags(post.tags, {
      show_count: false,
      style: 'link',
      class: 'post__tag'
    }) %>
  </span>
<% } %>
