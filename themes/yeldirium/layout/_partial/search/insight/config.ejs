<script>
  window.SEARCH_CONFIG = {
    url: '<%- url_for("/content.json")%>',
    resultsPerSection: '<%- theme.search.resultsPerSection %>',
    translation: {
      posts: '<%= __("search.posts") %>',
      pages: '<%= __("search.pages") %>',
      categories: '<%= __("search.categories") %>',
      tags: '<%= __("search.tags") %>',
      untitled: '<%= __("search.untitled") %>',
    },
    weights: {
      post: <%- JSON.stringify(theme.search.weights.post) || "undefined" %>,
      page: <%- JSON.stringify(theme.search.weights.page) || "undefined" %>,
      category: <%- JSON.stringify(theme.search.weights.category) || "undefined" %>,
      tag: <%- JSON.stringify(theme.search.weights.tag) || "undefined" %>
    }
  };
</script>
